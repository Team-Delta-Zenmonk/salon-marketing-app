export interface PlanItem {
  id: string;
  name: string;
  badge: string;
  priceDisplay: string;
  periodDisplay: string;
  subtext: string;
  tagline: string;
  popular: boolean;
  features: string[];
  ctaText: string;
  ctaPath: string;
  isDemo: boolean;
}

